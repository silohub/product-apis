description: Success
content:
  application/json:
    schema:
      type: object
      description: Permissions
      properties:
        permissions:
          type: array
          items:
            type: string
    examples:
      bffRetailer:
        $ref: ../../examples/users/User1Permissions.yaml
