type: object
description: |
  Referencia externa de la entidad.

  Siempre consiste en un tipo de referencia y el valor de la ella.
properties:
  refType:
    $ref: RefType.yaml
  refValue:
    $ref: RefValue.yaml
required:
  - refType
  - refValue