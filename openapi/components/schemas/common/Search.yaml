name: Search
type: object
description: Search parameters
properties:
  queryId:
    type: string
  from:
    type: integer
  to:
    type: integer
  maxRows:
    type: integer
  filters:
    type: array
    items:
      type: string
required:
  - from
  - to
  - maxRows
