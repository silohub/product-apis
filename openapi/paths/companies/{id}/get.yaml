get:
  operationId: getCompanyById
  summary: Obtiene una empresa por id
  parameters:
    - name: id
      in: path
      description: Id único de la empresa
      required: true
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            $ref: ../../../components/schemas/Company.yaml
          example:
            companyId: user1
            legalName: SILOHUB AAA
    '403':
      description: Forbidden
    '404':
      description: User not found
